<div class="stub-banner" data-testid="stub-banner" role="alert">
  <strong>STUB TRANSCRIPTION</strong> — this is placeholder output, not a real
  transcription. Supply <code>ANTHROPIC_API_KEY</code> (and set
  <code>TRANSCRIBE_PROVIDER=anthropic</code>) for real results.
</div>
